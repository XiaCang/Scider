# 论文发现与文档解析系统实现文档

## 一、核心功能实现概述

本人在Scider项目中主要负责论文发现模块和文档解析模块的实现，具体包括：

1. **论文检索与导入系统** (`src/backend/app/api/routes/discover.py`)
   - Semantic Scholar API 集成
   - 四个核心接口的实现
   - 分页、筛选、排序功能

2. **异步文档解析系统** (`src/backend/app/tasks/parse_task.py`)
   - Celery 任务实现
   - PDF 文本提取与清洗
   - 与 LLM 处理的衔接

3. **Semantic Scholar 服务层** (`src/backend/app/services/semantic_scholar.py`)
   - API 客户端封装
   - 限流处理与重试机制
   - 数据格式化

4. **单元测试** (`src/backend/tests/test_semantic_scholar.py`)
   - 检索逻辑测试
   - 异常处理测试

## 二、接口功能实现逻辑详解

### 2.1 论文检索接口 (`/discover/search`)

**功能描述**：
- 调用 Semantic Scholar API 进行关键词检索
- 支持年份范围筛选、来源类型筛选
- 支持多种排序方式（相关度、引用数、发表时间）
- 分页返回结果

**实现逻辑**：
```python
# 核心流程
1. 参数验证和日志记录
2. 调用 semantic_scholar.search_papers()
3. 异步线程执行避免阻塞
4. 标注用户文库中已存在的论文
5. 返回格式化结果
```

**关键技术点**：
- **年份筛选**：通过 SS API 原生支持，参数格式 `year_from-year_to`
- **来源类型筛选**：客户端过滤（因 SS API 不支持），基于 venue 和 publicationTypes 推断
- **排序实现**：相关度由 SS 默认保证，引用数和日期在客户端排序
- **文库标注**：批量查询 DOI 和标题，避免 N+1 查询问题

### 2.2 单篇导入接口 (`/discover/import`)

**功能描述**：
- 将检索到的论文导入用户文库
- 自动下载 PDF（如有开放获取链接）
- 创建 Paper 记录并触发异步解析任务

**实现逻辑**：
```python
# 核心流程
1. JWT 鉴权获取用户信息
2. 查重检查（基于 DOI）
3. 下载 PDF 并计算 MD5 哈希
4. 创建 Paper 记录（状态：PENDING_PARSING）
5. 有 PDF 时触发 parse_pdf_task
```

**关键技术点**：
- **PDF 下载**：使用 httpx.AsyncClient，支持重定向，30秒超时
- **文件存储**：按 MD5 哈希命名，避免重复存储
- **任务触发**：只有成功下载 PDF 才触发解析任务
- **状态管理**：清晰的状态流转（PENDING_PARSING → PARSING → PENDING_EXTRACTION）

### 2.3 参考文献接口 (`/discover/references/{semantic_id}`)

**功能描述**：
- 获取指定论文的参考文献（上游文献）
- 标注哪些文献已在用户文库中

**实现逻辑**：
```python
# 核心流程
1. 用户鉴权
2. 调用 semantic_scholar.get_references()
3. 批量标注文库状态
4. 返回结果
```

**关键技术点**：
- **Semantic ID 解析**：优先使用 DOI，无 DOI 时按标题搜索
- **批量标注**：使用 `_annotate_in_library()` 函数，支持 DOI 和标题两种匹配方式

### 2.4 引用文献接口 (`/discover/citations/{semantic_id}`)

**功能描述**：
- 获取引用指定论文的文献（下游文献）
- 标注哪些文献已在用户文库中

**实现逻辑**：与参考文献接口类似，调用 `semantic_scholar.get_citations()`

## 三、文档解析与联调实现

### 3.1 PDF 解析 Celery 任务

**任务流程**：
```python
parse_pdf_task(paper_id, pdf_path):
  1. 更新状态为 PARSING
  2. 提取 PDF 文本（不限制长度）
  3. 文本清洗和截断
  4. 更新状态为 PENDING_EXTRACTION
  5. 触发 extract_key_points_task
```

**文本清洗逻辑**：
```python
def clean_pdf_text(text: str) -> str:
    # 修复连字符断行：trans-\nformer → transformer
    text = re.sub(r"-\n(\w)", r"\1", text)

    # 合并多余空行：\n\n\n → \n\n
    text = re.sub(r"\n{3,}", "\n\n", text)

    # 压缩行内空白：多个空格 → 单个空格
    text = re.sub(r"[ \t]+", " ", text)

    return text.strip()
```

**关键技术点**：
- **异步数据库操作**：使用 asyncio.run() 在同步 Celery 任务中执行异步 DB 操作
- **错误处理**：PDF 提取失败时重试，空文本时标记为失败
- **长度限制**：截断至 8000 字符，防止 LLM token 超限
- **任务链**：解析完成后自动触发 LLM 提取任务

### 3.2 Semantic Scholar 服务层

**核心功能**：
- **search_papers()**：关键词检索，支持筛选和排序
- **get_references()**：获取参考文献
- **get_citations()**：获取引用文献

**限流处理实现**：
```python
def _request(method: str, url: str, **kwargs) -> dict:
    for attempt in range(_MAX_RETRIES):
        time.sleep(_MIN_INTERVAL)  # 主动限速

        try:
            # 发送请求
            if r.status_code == 429:
                # 读取 Retry-After 头，指数退避兜底
                wait = int(r.headers.get("Retry-After", _RETRY_DELAY * (2 ** attempt)))
                time.sleep(wait)
                continue

        except Exception as e:
            # 指数退避重试
            wait = _RETRY_DELAY * (2 ** attempt)
            time.sleep(wait)
```

**关键技术点**：
- **主动限速**：每次请求间隔至少 1.1 秒，避免触发匿名限制
- **429 处理**：优先使用服务器返回的 Retry-After，兜底用指数退避
- **API Key 支持**：配置 API Key 可提升速率限制
- **数据格式化**：统一响应格式，推断来源类型

## 四、遇到的问题与解决方案

### 4.1 Semantic Scholar API 限流问题 (HTTP 429)

**问题描述**：
- 匿名用户访问频率限制严格
- 频繁请求导致 429 错误

**解决方案**：
1. **申请 API Key**：联系 Semantic Scholar 申请 API Key，显著提升访问限额
2. **主动限速**：实现 `_MIN_INTERVAL = 1.1` 秒的最小请求间隔
3. **智能重试**：
   - 读取 `Retry-After` 响应头
   - 指数退避算法兜底
   - 最多重试 5 次

**代码体现**：
```python
# 配置 API Key
if settings.SEMANTIC_SCHOLAR_API_KEY:
    h["x-api-key"] = settings.SEMANTIC_SCHOLAR_API_KEY

# 主动限速
time.sleep(_MIN_INTERVAL)

# 智能重试
wait = int(r.headers.get("Retry-After", _RETRY_DELAY * (2 ** attempt)))
```

### 4.2 PDF 文本提取质量问题

**问题描述**：
- PDF 格式复杂，提取文本存在断行、乱码等问题
- 影响后续 LLM 处理效果

**解决方案**：
1. **连字符修复**：`trans-\nformer` → `transformer`
2. **空行合并**：多个连续空行合并为两个
3. **空白压缩**：多个空格/制表符合并为单个空格
4. **长度截断**：保留开头最有价值部分，限制 LLM 输入

### 4.3 异步任务与数据库交互

**问题描述**：
- Celery 任务是同步的，但数据库操作是异步的
- 需要更新任务状态

**解决方案**：
- 使用 `asyncio.run()` 在同步任务中执行异步数据库操作
- 封装独立的异步函数处理状态更新
- 异常时降级处理，避免任务失败

## 五、单元测试覆盖

### 5.1 测试范围

**基础检索测试**：
- 分页信封验证
- 参数传递正确性
- 字段格式化

**筛选功能测试**：
- 年份筛选（单边界、双边界、无边界）
- 来源类型筛选（conference、journal、arXiv）
- 来源类型推断逻辑

**排序功能测试**：
- 按引用数排序
- 按发表时间排序

**异常处理测试**：
- 429 限流重试
- 超过最大重试次数抛出异常

**上下游文献测试**：
- get_references 功能
- get_citations 功能
- 空值过滤

### 5.2 测试数据设计

```python
PAPERS = [
    _paper("p1", "Transformer", 2017, "NeurIPS", ["Conference"], "10.1/t", 90000),
    _paper("p2", "BERT", 2019, "ACL", ["Conference"], "10.1/b", 50000),
    _paper("p3", "GPT-4", 2023, "arXiv", ["JournalArticle"], "10.1/g", 10000),
    _paper("p4", "Old Paper", 2010, "Nature", ["JournalArticle"], "10.1/o", 500),
]
```

## 六、评委可能提问与回答准备

### 6.1 技术实现类问题

**Q1: 为什么选择 Semantic Scholar 而不是其他学术搜索引擎？**

A: 选择 Semantic Scholar 主要基于以下几点考虑：
1. **免费且开放**：提供免费的 API，适合学术研究项目
2. **数据质量高**：涵盖 arXiv、会议论文、期刊论文等多来源
3. **API 友好**：提供丰富的字段（citation count、references、citations 等）
4. **开放获取支持**：提供 openAccessPdf 字段，便于 PDF 下载
5. **学术影响力**：由 AI2 开发，在学术界有良好声誉

**Q2: 如何处理 API 限流问题？你们的解决方案有什么特点？**

A: 我们采用了多层次的限流处理策略：

1. **预防层面**：
   - 主动限速：每次请求间隔至少 1.1 秒
   - 申请 API Key：提升访问限额

2. **应对层面**：
   - 智能重试：读取服务器返回的 Retry-After 头
   - 指数退避：兜底重试策略，避免雪崩效应
   - 最大重试次数限制：防止无限重试

3. **用户体验层面**：
   - 异步处理：API 限流不影响用户界面响应
   - 错误提示：清晰的错误信息反馈

**Q3: PDF 解析为什么选择 PyMuPDF？清洗逻辑的设计思路是什么？**

A: 选择 PyMuPDF 的原因：
1. **提取质量高**：对复杂 PDF 格式支持良好
2. **性能优秀**：相比其他库速度更快
3. **功能完整**：支持文本、图片、元数据提取

清洗逻辑设计思路：
1. **连字符修复**：学术文献常见断行问题，影响语义理解
2. **空行合并**：PDF 格式转换产生的多余空行，影响阅读
3. **空白压缩**：统一格式，减少噪声
4. **长度截断**：LLM 输入限制，保留最有价值的前部内容

### 6.2 架构设计类问题

**Q4: 为什么使用 Celery 而不是其他异步任务方案？**

A: 选择 Celery 的主要考虑：
1. **成熟稳定**：业界广泛使用的分布式任务队列
2. **重试机制**：内置任务重试、失败处理机制
3. **监控友好**：支持 Flower 等监控工具
4. **扩展性强**：支持分布式部署，易于水平扩展
5. **与 FastAPI 集成良好**：异步任务与同步 API 的桥梁

**Q5: 数据库状态流转的设计考虑是什么？**

A: 状态流转设计基于以下考虑：

```
PENDING_PARSING → PARSING → PENDING_EXTRACTION → (SUCCESS/FAILED)
```

1. **清晰的责任划分**：每个状态对应明确的处理阶段
2. **错误隔离**：解析失败不影响其他任务
3. **进度可见**：用户可了解论文处理进度
4. **恢复能力**：失败状态可重新触发处理

### 6.3 性能优化类问题

**Q6: 如何优化大量论文的文库标注性能？**

A: 我们采用了批量查询策略避免 N+1 问题：

1. **DOI 批量查询**：一次性查询所有有 DOI 的论文
2. **标题批量查询**：使用 `LOWER(title) IN (...)` 查询无 DOI 论文
3. **内存去重**：使用集合数据结构快速查找

```python
# 批量查询 DOI
rows = await session.scalars(
    select(Paper.doi).where(Paper.doi.in_(dois), Paper.user_id == user_id)
)
in_library_dois = set(rows.all())
```

**Q7: 为什么限制 LLM 输入为 8000 字符？**

A: 这个限制基于多方面考虑：
1. **成本控制**：LLM API 按 token 计费，限制长度控制成本
2. **性能考虑**：短文本处理更快，用户体验更好
3. **内容质量**：学术论文开头通常包含摘要、引言等关键信息
4. **实际测试**：8000 字符通常足够覆盖摘要、引言、方法概述

### 6.4 异常处理类问题

**Q8: 系统如何处理各种异常情况？**

A: 我们建立了分层的异常处理机制：

1. **API 层异常**：
   - 网络错误：重试机制
   - 429 限流：智能等待重试
   - 其他 HTTP 错误：抛出明确异常

2. **业务层异常**：
   - PDF 下载失败：记录警告，继续创建元数据记录
   - 文本提取失败：标记任务失败，可重试
   - 数据库错误：事务回滚，状态一致性

3. **用户层异常**：
   - 清晰的错误提示
   - 适当的状态码（401、404、409、502等）

### 6.5 扩展性类问题

**Q9: 系统如何支持更多的学术搜索引擎？**

A: 我们的设计具有良好的扩展性：

1. **服务层抽象**：`semantic_scholar.py` 封装了所有 API 相关逻辑
2. **统一接口**：定义标准的论文数据格式
3. **配置化**：API Key、URL 等通过配置管理
4. **插件化设计**：新增搜索引擎只需实现相同接口

**Q10: 如何支持更多的文档格式？**

A: 当前系统主要考虑：
1. **核心聚焦**：PDF 是学术文献的主要格式
2. **扩展接口**：`extract_text_from_pdf` 函数可替换为通用文档提取器
3. **格式检测**：可添加文件类型检测和相应处理器
4. **异步处理**：不同格式可并行处理

## 七、总结

本实现构建了一个完整的论文发现与解析系统，具有以下特点：

1. **功能完整**：覆盖检索、导入、上下游文献发现全流程
2. **健壮性强**：完善的异常处理和重试机制
3. **性能优化**：异步处理、批量操作、智能限流
4. **可维护性好**：清晰的代码结构、完整的单元测试
5. **用户体验佳**：清晰的状态流转、友好的错误提示

通过 Semantic Scholar API 集成，解决了学术文献检索的难题；通过 Celery 异步任务，实现了高效的文档处理；通过细致的异常处理，保证了系统的稳定性。
